export default {
	darkMode: "class", // Enable dark mode using class
	content: [
		"./src/**/*.{js,jsx,ts,tsx}", // Include all your component files
	],
	theme: {
		extend: {}, // No need to extend unless you want customizations
	},
	plugins: [], // No plugins needed unless you want animations or other features
};
